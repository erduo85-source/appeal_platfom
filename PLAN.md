# 账号申诉后台：游戏接入配置方案

## Summary

新增入口按钮“游戏接入配置”，点击后打开右侧 900px 抽屉，用于动态配置用户端展示的申诉游戏列表，并同步影响后台“申诉游戏”筛选项。所有新增、编辑、删除、拖拽排序均先在抽屉内暂存，点击抽屉底部“保存”后统一生效，线上约 5 分钟后同步。

## Key Changes

- 入口按钮文案改为“游戏接入配置”，放在页眉标题区域右侧。
- 抽屉标题为“游戏接入配置”，1920 宽屏下宽度固定 900px。
- 抽屉关闭按钮放在抽屉左上侧，超出抽屉内容区域，紧贴抽屉左侧边缘，使用蓝色方形按钮样式。
- 抽屉包含：
  - 提示区：标题为“配置提示：”，文案为“配置保存后 5 分钟，同步更新线上账号申诉和账号找回的游戏列表展示，请谨慎操作。”。
  - “游戏列表”区域。
  - 账号体系下拉筛选，默认展示“游卡”。
  - 底部固定“取消 / 保存”按钮。
- 抽屉关闭规则：
  - 点击关闭或取消时，先校验是否存在未保存更新。
  - 如无更新，直接关闭抽屉。
  - 如有更新，弹出确认：“关闭后将重置编辑内容，请确认是否关闭。”。
  - 用户确认后关闭并清空暂存编辑；用户取消则留在抽屉继续编辑。
- 列表字段：
  - 拖拽手柄、游戏（LOGO + 游戏名称）、appid、账号体系、最近更新时间、最近更新人、操作。
  - “接入其他游戏”按钮与账号体系筛选同一行展示，并右对齐。
  - 仅允许通过最左侧拖拽手柄排序。
  - 游戏列表过长时支持垂直滚动查看，设计稿中展示滚动条。
  - 删除需二次确认，确认后仅暂存删除，最终由抽屉“保存”提交。
- “接入其他游戏”和“编辑”打开居中弹窗。
  - 接入弹窗：项目选择、展示游戏名、验证苹果订单、苹果包名、KeyID、IssuerID、PrivateKey。
  - 弹窗标题居中展示。
  - 编辑弹窗：项目选择可修改，已选态使用多选 tag 样式；可修改展示游戏名和苹果验单配置。
  - 项目选择下拉展示全部项目，已接入项目禁用并标注“已接入”。
  - 如未找到所需项目，提示联系 SDK 部门添加。
  - 游戏 LOGO 不在弹窗内配置，默认读取首个选择项目在平台服务中心“项目概览”中的已有 LOGO。
  - 弹窗主按钮为“确认”，只回写抽屉暂存列表，不直接同步线上。
- 页面状态需补充独立设计稿 Frame：
  - 编辑申诉游戏弹窗。
  - 删除二次确认。
  - 取消/关闭二次确认。
  - 保存二次确认。

## Mock Data

抽屉“游戏列表”使用以下 mock 数据：

| 游戏名称 | appid |
|---|---|
| 三国杀移动版 | 10100001 |
| 三国杀一将成名 | 10100011 |
| 三国杀OL | 10100002 |
| 三国杀名将传 | 10100005 |
| 自在西游 | 10100032 |
| 指间山海 | 10100042 |
| 三国杀武将觉醒 | 10100080 |
| 欢乐三国杀 | 10100031 |
| 怒焰三国杀 | 10100004 |
| 狼人对决 | 10100010 |

项目选择弹窗 mock 选中项与展示游戏名默认使用“三国杀移动版 / 10100001”，编辑态项目选择已选项展示为多选 tag；下拉中展示部分候选并保留“已接入”禁用态示例。账号体系、更新时间、更新人继续使用现有 mock 风格，不引入真实接口含义。

## Interfaces / Data

- 配置列表项至少包含：
  - `appid`
  - `appids`
  - `gameName`
  - `logoUrl`
  - `accountSystem`
  - `appleVerification`
  - `sortOrder`
  - `updatedAt`
  - `updatedBy`
- 后台筛选项使用保存后的配置列表生成。
- 历史申诉列表仍展示原记录中的游戏信息，不因配置删除而隐藏历史数据。
- 申诉游戏仅生效于对应账号体系的账号申诉和账号找回服务。

## Test Plan

- 点击“游戏接入配置”能打开 900px 右侧抽屉。
- 抽屉内账号体系筛选展示在“游戏列表”标题下方，“接入其他游戏”按钮与筛选行右对齐。
- 有未保存更新时，点击关闭或取消出现二次确认文案：“关闭后将重置编辑内容，请确认是否关闭。”。
- 新增游戏后，弹窗“确认”只更新抽屉列表；抽屉保存后才提示同步成功。
- 编辑展示游戏名、项目选择或苹果验单配置后，列表展示立即按暂存值更新。
- 删除游戏需二次确认，取消抽屉可放弃删除。
- 拖拽手柄排序后，保存结果按新顺序展示。
- 配置列表超过可视高度时可垂直滚动，滚动条可见。
- 已接入项目在下拉中置灰不可选。
- 开启苹果验单但苹果包名、KeyID、IssuerID、PrivateKey 未填完整时，弹窗“确认”校验失败。
- 删除、取消/关闭、保存均有二次确认状态稿。
- 保存后后台“申诉游戏”筛选项同步为最新配置列表。
- Figma 视觉自检：抽屉、表格、弹窗在 1920 宽屏下无遮挡、异常换行或按钮错位。

## Assumptions

- 本期不新增登录、权限、审批流、发布流、操作日志页或数据库设计。
- “5 分钟同步线上”仅作为提示和成功反馈文案，不在前端实现倒计时。
- 账号体系由项目基础信息映射得到，当前默认展示“游卡”。
- 参考图、`drawer_modal_spec.md` 和 Figma 节点 `465:6` 作为视觉方向，以 Figma 最新设计稿为准。
